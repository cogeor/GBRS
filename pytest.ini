[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    baseline: marks tests that compare against baseline models

addopts =
    -v
    --strict-markers
    --tb=short
