% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{gbrs_bootstrap}
\alias{gbrs_bootstrap}
\title{Bootstrap GBRS Model}
\usage{
gbrs_bootstrap(
  formula,
  data,
  n_bootstrap = 10,
  n_max = 100,
  lr = 0.1,
  n_quantiles = 10,
  batch_size = 0,
  objective = "auto",
  seed = NULL
)
}
\arguments{
\item{formula}{A formula specifying the model.}

\item{data}{A data frame containing the variables in the formula.}

\item{n_bootstrap}{Integer. Number of bootstrap iterations (default: 10).}

\item{n_max}{Integer. Maximum boosting iterations per fit (default: 100).}

\item{lr}{Numeric. Learning rate (default: 0.1).}

\item{n_quantiles}{Integer. Number of quantile thresholds (default: 10).}

\item{batch_size}{Integer. Batch size for fitting (default: 0).}

\item{objective}{Character. Objective function ("auto", "continuous", "binary", "survival").}

\item{seed}{Integer. Random seed for reproducibility (default: NULL).}
}
\value{
An object of class \code{"gbrs_bootstrap"} containing:
\item{thresholds}{List of pre-computed thresholds for each feature}
\item{results}{List of model weights from each bootstrap iteration}
\item{n_bootstrap}{Number of bootstrap samples}
\item{objective}{The objective function used}
}
\description{
Fits a GBRS model multiple times with bootstrap samples to compute confidence
intervals for weights. Uses pre-computed thresholds from the full dataset to
ensure consistent split points across all bootstrap samples.
}
\examples{
result <- gbrs_bootstrap(mpg ~ wt + hp, data = mtcars, n_bootstrap = 10)
print(result)

}
